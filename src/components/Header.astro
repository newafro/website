---
import { u } from '../lib/url';

const nav = [
  { href: '/', label: 'HOME' },
  { href: '/agency', label: 'THE AGENCY' },
  { href: '/community', label: 'COMMUNITY' },
  { href: '/archive', label: 'THE ARCHIVE' },
  { href: '/projects', label: 'PROJECTS' },
  { href: '/events', label: 'EVENTS' },
];
---

<header class="site-header">
  <div class="header-inner">
    <a class="brand-link" href={u('/')} aria-label="New Afro home">
      <img src={u('/logo.png')} alt="" width="3337" height="2363" loading="eager" decoding="async" />
    </a>
  </div>

  <button
    class="nav-toggle"
    aria-controls="primary-nav"
    aria-expanded="false"
    type="button"
  >
    <span class="visually-hidden">Toggle menu</span>
    <span class="bar" aria-hidden="true"></span>
    <span class="bar" aria-hidden="true"></span>
    <span class="bar" aria-hidden="true"></span>
  </button>

  <nav id="primary-nav" class="nav-panel" aria-label="Primary">
    <div class="nav-panel__inner">
      <ul>
        {nav.map(item => (
          <li><a href={u(item.href)}>{item.label}</a></li>
        ))}
      </ul>
    </div>
  </nav>
</header>

<script>
  const toggle = document.querySelector<HTMLButtonElement>('.nav-toggle');
  const nav = document.getElementById('primary-nav');

  if (toggle && nav) {
    const setOpen = (open: boolean) => {
      toggle.setAttribute('aria-expanded', String(open));
      nav.classList.toggle('is-open', open);
      document.body.classList.toggle('nav-locked', open);
    };

    toggle.addEventListener('click', () => {
      setOpen(toggle.getAttribute('aria-expanded') !== 'true');
    });

    nav.addEventListener('click', (event) => {
      if (event.target === nav || event.target instanceof HTMLAnchorElement) {
        setOpen(false);
      }
    });
  }
</script>

<style>
  .site-header {
    position: relative;
    z-index: 80;
    background: #fff;
    color: #000;
  }

  .header-inner {
    width: 100%;
    height: 92px;
    padding: 0 clamp(20px, 3vw, 40px);
    box-sizing: border-box;
    display: flex;
    align-items: center;
    justify-content: flex-start;
  }

  .brand-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 58px;
    min-height: 52px;
    color: #000;
    line-height: 0;
  }

  .brand-link img {
    width: 58px;
    height: auto;
    display: block;
    filter: brightness(0);
  }

  .nav-toggle {
    width: 48px;
    height: 48px;
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6px;
    border: 0;
    background: transparent;
    color: #000;
    cursor: pointer;
    position: fixed;
    top: 23px;
    right: 24px;
    flex: 0 0 48px;
    z-index: 90;
    transition: color 180ms ease;
  }

  .bar {
    width: 39px;
    height: 4px;
    display: block;
    background: currentColor;
    transition: transform 180ms ease, opacity 180ms ease;
  }

  .nav-toggle[aria-expanded="true"] {
    top: clamp(28px, 6.8vw, 70px);
    left: clamp(28px, 19vw, 400px);
    right: auto;
    color: rgba(255, 249, 235, 0.92);
  }

  .nav-toggle[aria-expanded="true"] .bar {
    width: 34px;
    height: 2px;
  }

  .nav-toggle[aria-expanded="true"] .bar:nth-child(2) {
    transform: translateY(10px) rotate(45deg);
  }

  .nav-toggle[aria-expanded="true"] .bar:nth-child(3) {
    opacity: 0;
  }

  .nav-toggle[aria-expanded="true"] .bar:nth-child(4) {
    transform: translateY(-10px) rotate(-45deg);
  }

  .nav-panel {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 70;
    overflow: hidden auto;
    background:
      radial-gradient(circle at 74% 12%, rgba(153, 105, 47, 0.95), transparent 46%),
      linear-gradient(105deg, #1f1203 0%, #2d1804 28%, #58340f 58%, #8a622e 100%);
    opacity: 0;
    pointer-events: none;
    transition: opacity 220ms ease;
  }

  @supports (height: 100dvh) {
    .nav-panel {
      height: 100dvh;
    }
  }

  .nav-panel.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  .nav-panel__inner {
    box-sizing: border-box;
    width: 100%;
    min-height: 100%;
    display: flex;
    align-items: center;
    padding: clamp(88px, 12vw, 140px) 24px clamp(48px, 7vw, 96px);
  }

  .nav-panel ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: clamp(20px, 3.8vw, 44px);
    text-align: left;
    margin-left: clamp(28px, 19vw, 400px);
  }

  .nav-panel a {
    font-family: var(--na-font-display);
    font-size: clamp(38px, 3.8vw, 54px);
    font-style: italic;
    font-weight: 300;
    line-height: 0.9;
    letter-spacing: 0;
    color: rgba(255, 249, 235, 0.95);
    text-decoration: none;
  }

  .nav-panel a:hover {
    color: #fff;
  }

  @media (max-width: 760px) {
    .nav-toggle {
      right: 22px;
    }

    .nav-toggle[aria-expanded="true"] {
      top: 26px;
      left: 22px;
      right: auto;
    }

    .nav-panel {
      background:
        radial-gradient(circle at 92% 8%, rgba(126, 88, 42, 0.82), transparent 50%),
        linear-gradient(130deg, #211204 0%, #351b05 58%, #6b4617 100%);
    }

    .nav-panel__inner {
      align-items: center;
      padding: 96px 28px 56px;
    }

    .nav-panel ul {
      margin-left: 0;
      gap: 26px;
    }

    .nav-panel a {
      font-size: clamp(36px, 14vw, 52px);
    }
  }

  @media (max-height: 760px) and (min-width: 761px) {
    .nav-panel__inner {
      padding-top: 72px;
      padding-bottom: 44px;
    }

    .nav-panel ul {
      gap: 20px;
    }

    .nav-panel a {
      font-size: clamp(34px, 3.4vw, 48px);
    }
  }

  :global(.nav-locked) {
    overflow: hidden;
  }
</style>
